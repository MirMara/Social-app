// import { render } from '@testing-library/react';
// import Header from './Header';

// test("checks markup in <Header /> component", () => {
//     const {container} = render(<Header />);

//     const header = container.querySelector("header");

//     expect(header).toBeTruthy();

// });